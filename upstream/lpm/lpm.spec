# -*- mode: python ; coding: utf-8 -*-

import sys
from pathlib import Path


spec_path = Path(sys.argv[0]).resolve()
spec_dir = spec_path.parent
script = spec_dir / "lpm.py"

a = Analysis(
    [script.as_posix()],
    pathex=[spec_dir.as_posix()],
    binaries=[],
    datas=[],
    hiddenimports=[],
    hookspath=[],
    hooksconfig={},
    runtime_hooks=[],
    excludes=[],
    noarchive=False,
    optimize=0,
)
pyz = PYZ(a.pure)

exe = EXE(
    pyz,
    a.scripts,
    a.binaries,
    a.datas,
    [],
    name="lpm",
    debug=False,
    bootloader_ignore_signals=False,
    strip=False,
    upx=True,
    upx_exclude=[],
    runtime_tmpdir=None,
    console=True,
    disable_windowed_traceback=False,
    argv_emulation=False,
    target_arch=None,
    codesign_identity=None,
    entitlements_file=None,
)
