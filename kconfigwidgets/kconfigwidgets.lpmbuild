# LPM build script for kconfigwidgets
NAME="kconfigwidgets"
VERSION="1.0.0"
RELEASE="1"
ARCH="${ARCH:-x86_64}"
SUMMARY="Placeholder package for kconfigwidgets dependency"
URL="https://example.com/kconfigwidgets"
LICENSE=""
SOURCE=()
SHA256SUMS=()
PATCHES=()
REQUIRES=()
PROVIDES=("kconfigwidgets")
CONFLICTS=()
OBSOLETES=()
RECOMMENDS=()
SUGGESTS=()

prepare() {
  set -euxo pipefail
  cd "$SRCROOT"
  :
}

build() {
  set -euxo pipefail
  cd "$SRCROOT"
  :
}

staging() {
  set -euxo pipefail
  cd "$SRCROOT"
  install -d "$pkgdir/usr/share/doc/kconfigwidgets"
  printf 'Placeholder package for kconfigwidgets\n' >"$pkgdir/usr/share/doc/kconfigwidgets/README"
}
